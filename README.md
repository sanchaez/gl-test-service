# Google Repo local_manifests test
## how to test
- Clone the `manifest` branch into the `.repo/local_manifests` folder.
- Do `repo init` and `repo sync`
- Build this module using `make GlTestService`

